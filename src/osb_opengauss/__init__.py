"""osb-opengauss: an Open Service Broker API broker for openGauss/GaussDB.

Service instances are logical databases (openGauss ``CREATE DATABASE``) that act
as tenants; bindings are scoped user accounts (``CREATE USER``) whose access is
limited to exactly one logical database.
"""

__version__ = "0.1.0"
